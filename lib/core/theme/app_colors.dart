import 'package:flutter/material.dart';

class AppColors {
  // Avant-Garde Futuristic High Fashion (Dark Mode)
  static const Color background = Color(0xFF0A0A0A); // Vantablack aesthetic
  static const Color cardDark = Color(0xFF121212); // Deep subtle grey
  static const Color cardLight = Color(0xFF1E1E1E); // Elevated panel

  // Metallic Accents (Chrome & Muted Rose Gold)
  static const Color primary = Color(
    0xFFE5E5E5,
  ); // Chrome Silver (Main Text/Icons)
  static const Color secondary = Color(0xFF5A5A5A); // Slate Grey
  static const Color accent = Color(0xFFDAB39A); // Muted Rose Gold / Champagne

  static const Color textBody = Color(0xFF888888); // Elegant muted text
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
