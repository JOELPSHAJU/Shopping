import 'package:flutter/material.dart';

class AppColors {
  // Avant-Garde Futuristic High Fashion (Dark Mode)
  static const Color background = Color(0xFF0A0A0A);
  static const Color cardDark = Color(0xFF121212);
  static const Color cardLight = Color(0xFF1E1E1E);

  // Editorial Minimalist (Light Mode)
  static const Color lightBackground = Color(0xFFF8F5F2); // Warm Ivory
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightBorder = Color(0xFFE5E2DD);
  static const Color lightTextBody = Color(0xFF666666);
  static const Color lightTextTitle = Color(0xFF1A1A1A);

  // Metallic Accents (Chrome & Muted Rose Gold)
  static const Color primary = Color(0xFFE5E5E5);
  static const Color secondary = Color(0xFF5A5A5A);
  static const Color accent = Color(0xFFDAB39A);

  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Legacy/Common
  static const Color textBody = Color(0xFF888888);
}
